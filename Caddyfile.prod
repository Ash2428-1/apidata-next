staging.apihub.xtend.co {
    # Xtend Hub v4 app — local Docker container on this server
    reverse_proxy xtend-hub-app:3000
}

apihub.xtend.co {
    reverse_proxy app:3000
}

# Fallback for direct IP access
:80 {
    reverse_proxy app:3000
}
