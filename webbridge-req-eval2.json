{"action":"evaluate","args":{"code":"(() => { const html = document.documentElement.innerHTML; const matches = []; const urlPatterns = [/(https?:\\/\\/[^\\s\"'<>]+)/g]; urlPatterns.forEach(p => { let m; while ((m = p.exec(html)) !== null) { if (!matches.includes(m[1]) && (m[1].includes('api') || m[1].includes('supabase') || m[1].includes('wesbank') || m[1].includes('flickswitch') || m[1].includes('xtend'))) matches.push(m[1]); } }); return JSON.stringify(matches.slice(0,100)); })()"},"session":"xtend-api-research"}